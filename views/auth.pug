extends template.pug 
block main
  div
    form(action='http://localhost:3093/auth/product/create', method='POST', class="form")
      p створити новий пост
      label(for="title") Enter the title
      input(type='text', name='title', value='')

      label(for="description") Enter the description
      input(type='text', name='description', value='')

      label(for="product_type") Enter the product type
      input(type='text', name='product_type', value='')

      input(type='submit', value='Submit' class="submit-btn")
  //-   h5 Hello #{name}
  //- #app  {{message}}
  //- script.
  //-   const { createApp } = Vue
  //-   createApp({
  //-     data() {
  //-       return {
  //-         name: '#{name}',
  //-         city: '#{city}'
  //-       }
  //-     },
  //-     template: '<h1>OK, let`s go {{city}} #{city}</h1>'
  //-   }).mount('#app')

block scripts
  script(src="https://unpkg.com/vue@3/dist/vue.global.js")
  script(src="js/index.js")
